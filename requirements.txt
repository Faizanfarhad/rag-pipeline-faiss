# RAG Pipeline — Minimal dependencies for Render free tier (512MB)

# API
fastapi>=0.100.0
uvicorn>=0.29.0

# Embeddings + torch (CPU-only via extra index)
sentence-transformers>=2.2.0

# Vector Search (CPU)
faiss-cpu>=1.7.4

# Document Processing
pymupdf>=1.24.2
pymupdf4llm>=0.0.4
beautifulsoup4>=4.12.0
markdown>=3.5.0

# Chunking
llama-index-core>=0.10.0

# NLP utilities
nltk>=3.8.0
regex>=2024.0.0
numpy>=1.24.0

# NOTE: torch is pulled automatically by sentence-transformers.
# Removed: bitsandbytes (GPU), transformers/accelerate (not needed),
# torchvision, torchaudio, llama-index extras (not used in FAISS-only mode).